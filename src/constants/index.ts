import { base, heading, subheading, thai, prompt } from "./fonts";
import { PLANS } from "./plans";
import { NAV_LINKS } from "./links";
import { SUPPORTED_LANGUAGES } from "./countries";
import { FEATURES } from "./features";

export {
    base,
    heading,
    subheading,
    thai,
    prompt,
    PLANS,
    NAV_LINKS,
    SUPPORTED_LANGUAGES,
    FEATURES,
};